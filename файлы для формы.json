<!-- форма для amocrm -->
<!-- <script>
  window.addEventListener("DOMContentLoaded", function() {

    // get the form elements defined in your form HTML above

    var form = document.getElementById("my-form");
    var button = document.getElementById("my-form-button");
    var status = document.getElementById("my-form-status");

    // Success and Error functions for after the form is submitted

    function success() {
      form.reset();
      button.style = "display: none ";
      status.innerHTML = "Thanks!";
    }

    function error() {
      status.innerHTML = "Oops! There was a problem.";
    }

    // handle the form submission event

    form.addEventListener("submit", function(ev) {
      ev.preventDefault();
      var data = new FormData(form);
      ajax(form.method, form.action, data, success, error);
    });
  });

  // helper function for sending an AJAX request

  function ajax(method, url, data, success, error) {
    var xhr = new XMLHttpRequest();
    xhr.open(method, url);
    xhr.timeout = 10000;
    xhr.setRequestHeader("Accept", "application/json");
    xhr.onreadystatechange = function() {
      if (xhr.readyState !== XMLHttpRequest.DONE) return;
      if ( xhr.readyState == 4 && xhr.status != 200) {
        success(xhr.response, xhr.responseType);

      } else {
        error(xhr.status, xhr.response, xhr.responseType);
      }
    };
    xhr.send(data);
  }
</script>
<form  id="my-form" class="form"  action="https://forms.amocrm.ru/queue/add" method="POST">

      <input type="text" name="fields[name_1]" placeholder="Укажите, пожалуйста, ваше имя">

      <input type="email" name="fields[59113_1][83245]" placeholder="mail@amocrm.ru">

      <input type="tel"  name="fields[59109_1][83233]" placeholder="+7 (495) 000-00-00">

    <input type="hidden" name="form_id" id="form_id" value="687262">
    <input type="hidden" name="hash" value="74add7341bb5383fc564e4707b7cb922">

    <button id="my-form-button"  type="submit">Отправить
    </button>
    <p id="my-form-status"></p>
</form>  -->